trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self
    path: Übungsaufgaben
  - script: |
      chmod +x 5.sh
      5.sh
    displayName: 'Execute 5.sh script'
