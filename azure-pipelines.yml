trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self
    path: Übungsaufgaben
  - script: |
      chmod +x usageStatistics.sh
      usageStatistics.sh
    displayName: 'Execute usageStatistics.sh script'
