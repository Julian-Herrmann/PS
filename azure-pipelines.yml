trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

steps:
  - checkout: self

  - script: |
      chmod +x Übungsaufgaben/5.sh
      Übungsaufgaben/5.sh
    displayName: 'Execute 5.sh script'
