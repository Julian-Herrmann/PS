trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Hier wird das Bash-Skript aus dem Repository ausgeführt
      bash $(Build.SourcesDirectory)/Übungsaufgaben/5.sh
  displayName: 'Execute bash script'
