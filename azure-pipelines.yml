trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  scriptPath: 'Übungsaufgaben'  # Hier den Verzeichnispfad anpassen

steps:
  - checkout: self
    path: $(scriptPath)

  - script: |
      chmod +x $(scriptPath)/5.sh
      ./$(scriptPath)/5.sh
    displayName: 'Execute 5.sh script'
