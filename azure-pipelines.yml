trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Hier wird das Bash-Skript aus dem Repository ausgeführt
      bash ./5.sh
  displayName: 'Execute bash script'
